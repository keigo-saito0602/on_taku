<div class="mx-auto max-w-lg space-y-6">
  <div class="text-center">
    <h1 class="text-2xl font-semibold text-slate-900">アカウント設定</h1>
    <p class="mt-1 text-sm text-slate-500">表示名やメールアドレスを確認できます。</p>
  </div>

  <section class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <dl class="space-y-4 text-sm">
      <div class="flex items-baseline justify-between">
        <dt class="text-slate-500">氏名</dt>
        <dd class="font-medium text-slate-800"><%= @user.name %></dd>
      </div>
      <div class="flex items-baseline justify-between">
        <dt class="text-slate-500">表示名</dt>
        <dd class="font-medium text-slate-800"><%= @user.display_name %></dd>
      </div>
      <div class="flex items-baseline justify-between">
        <dt class="text-slate-500">メールアドレス</dt>
        <dd class="font-medium text-slate-800"><%= @user.email %></dd>
      </div>
      <div class="flex items-baseline justify-between">
        <dt class="text-slate-500">ロール</dt>
        <dd>
          <span class="inline-flex items-center rounded-full bg-sky-100 px-3 py-1 text-xs font-semibold text-sky-700"><%= @user.role_i18n %></span>
        </dd>
      </div>
    </dl>
    <div class="mt-6 flex justify-end">
      <%= button_to "ログアウト", session_path, method: :delete, class: "inline-flex items-center rounded-lg bg-rose-600 px-4 py-2 text-sm font-semibold text-white shadow transition hover:bg-rose-500" %>
    </div>
  </section>
</div>
