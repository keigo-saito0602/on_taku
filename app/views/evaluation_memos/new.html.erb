<div class="mx-auto max-w-lg space-y-6">
  <div>
    <h1 class="text-2xl font-semibold text-slate-900">評価メモCSVインポート</h1>
    <p class="mt-1 text-sm text-slate-500">CSVのヘッダーには「大項目」「中項目」「小項目」「実績/補足」が含まれている必要があります。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= form_with model: @evaluation_memo, url: event_evaluation_memos_path(@event), multipart: true, html: { class: "space-y-5" } do |form| %>
      <% if @evaluation_memo.errors.any? %>
        <div class="rounded-lg border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-800">
          <p class="font-semibold">インポートに失敗しました。</p>
          <ul class="mt-2 list-disc pl-5">
            <% @evaluation_memo.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div>
        <%= form.label :file, "CSVファイル", class: "block text-sm font-medium text-slate-700" %>
        <%= form.file_field :file, required: true, accept: ".csv", class: "mt-1 w-full rounded-lg border border-dashed border-slate-300 bg-slate-50 px-3 py-6 text-center text-sm text-slate-500 focus:border-sky-500 focus:outline-none focus:ring-2 focus:ring-sky-200" %>
      </div>

      <div class="flex justify-end gap-3">
        <%= link_to "イベント詳細に戻る", @event, class: "inline-flex items-center rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-medium text-slate-700 transition hover:bg-slate-50" %>
        <%= form.submit "インポート", class: "inline-flex items-center rounded-lg bg-sky-600 px-5 py-2 text-sm font-semibold text-white shadow transition hover:bg-sky-500" %>
      </div>
    <% end %>
  </div>
</div>
