<div class="mx-auto max-w-lg space-y-6">
  <div class="text-center">
    <h1 class="text-2xl font-semibold text-slate-900">アカウント登録</h1>
    <p class="mt-1 text-sm text-slate-500">必要事項を入力すると主催者アカウントが作成されます。</p>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= form_with model: @user, url: registration_path, html: { class: "space-y-5" } do |form| %>
      <% if @user.errors.any? %>
        <div class="rounded-lg border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-800">
          <p class="font-semibold">入力内容にエラーがあります。</p>
          <ul class="mt-2 list-disc pl-5">
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="grid gap-4 md:grid-cols-2">
        <div>
          <%= form.label :name, "氏名", class: "block text-sm font-medium text-slate-700" %>
          <%= form.text_field :name, required: true, class: "mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm shadow-sm focus:border-sky-500 focus:outline-none focus:ring-2 focus:ring-sky-200" %>
        </div>
        <div>
          <%= form.label :display_name, "表示名", class: "block text-sm font-medium text-slate-700" %>
          <%= form.text_field :display_name, required: true, class: "mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm shadow-sm focus:border-sky-500 focus:outline-none focus:ring-2 focus:ring-sky-200" %>
        </div>
      </div>

      <div>
        <%= form.label :email, "メールアドレス", class: "block text-sm font-medium text-slate-700" %>
        <%= form.email_field :email, required: true, autocomplete: "email", class: "mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm shadow-sm focus:border-sky-500 focus:outline-none focus:ring-2 focus:ring-sky-200" %>
      </div>

      <div>
        <%= form.label :password, "パスワード", class: "block text-sm font-medium text-slate-700" %>
        <%= form.password_field :password, required: true, autocomplete: "new-password", class: "mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm shadow-sm focus:border-sky-500 focus:outline-none focus:ring-2 focus:ring-sky-200" %>
        <p class="mt-1 text-xs text-slate-500">8〜32文字の英数字</p>
      </div>

      <div>
        <%= form.label :password_confirmation, "パスワード（確認）", class: "block text-sm font-medium text-slate-700" %>
        <%= form.password_field :password_confirmation, required: true, autocomplete: "new-password", class: "mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm shadow-sm focus:border-sky-500 focus:outline-none focus:ring-2 focus:ring-sky-200" %>
      </div>

      <div class="flex justify-end gap-3">
        <%= link_to "ログインに戻る", new_session_path, class: "inline-flex items-center rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-medium text-slate-700 transition hover:bg-slate-50" %>
        <%= form.submit "登録する", class: "inline-flex items-center rounded-lg bg-sky-600 px-5 py-2 text-sm font-semibold text-white shadow transition hover:bg-sky-500" %>
      </div>
    <% end %>
  </div>
</div>
